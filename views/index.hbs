<!-- search bar -->
<div class="container search-bar">
  <div class="search-bar-outer">
    <div class="search-bar-inner">
      <form action="/search">
        <h4 class="mb-2">尋找餐廳：</h4>
        <div class="input-group">
          <input type="text" name="keywords" class="form-control" placeholder="輸入餐廳、分類" value="{{keywords}}"
            aria-label="Restaurant Name..." value="" aria-describedby="search-button">
          <div class="input-group-append">
            <button class="btn btn-warning" type="submit" id="search-button"><i class="fas fa-search"></i></button>
          </div>
        </div>
      </form>
    </div>
  </div>
</div>

<!-- 新增餐廳 -->
<div class="text-left mx-3">
  <a href="/restaurants/new"><button class="btn btn-outline-danger rounded-pill mx-5 btn-lg">📝新增 </button></a>
</div>

<!-- restaurant list -->
<div class="container mt-5 px-md-5">
  <div class="row">
    {{#each restaurantsData}}
    <div class="col-12 col-sm-6 col-md-4">
      <div class="card mb-3">
        <img class="card-img-top" src="{{this.image}}" alt="{{this.name}}" />
        <div class="card-body p-3">
          <h6 class="card-title mb-1">{{this.name}}</h6>
      
          <div class="restaurant-category mb-1">
            <i class="fas fa-utensils pr-2"></i>
            {{this.category}}
          </div>
      
          <span class="badge badge-pill badge-danger font-weight-normal">
            {{this.rating}}
            <i class="fas fa-star fa-xs"></i>
          </span>
        </div>
        <div class="card-footer d-flex justify-content-end flex-wrap">
          <a href="/restaurants/{{this._id}}/edit"><button class="btn btn-outline-info text-gray btn-sm mr-1">編輯</button></a>
          <a href="/restaurants/{{this._id}}"><button class="btn btn-outline-info text-gray btn-sm mr-1">詳細</button></a>
          <form action="/restaurants/{{this._id}}?_method=DELETE" method="POST">
            <button type="submit" class="btn btn-outline-warning  btn-sm text-white">刪除</button>
          </form>
        </div>
      </div>
    </div>
    {{/each}}
  </div>
</div>